# Contributing

1. Create a branch from `main`.
2. Keep changes focused and update tests when API behavior changes.
3. Run `npm run lint`, `npm test`, and `npm run build` locally.
4. Open a pull request describing the behavior and verification performed.

Never commit `.env` files, production credentials, generated `data/`, or customer data.
